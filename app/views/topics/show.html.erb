<div class="container-fluid">
	<div class="row">
		<h1 class="topic-title">#<%= @topic.title %></h1>
		<div class="pull-right topic-buttons">
			<% if policy(@topic).update? %>
        <h4>
          <%= link_to 'Edit', edit_topic_path, class: "glyphicon glyphicon-pencil" %> |
          <%= link_to 'Back', topics_path, class: "glyphicon glyphicon-back" %>
        </h4>
      <% end %>
    </div>
  </div>
</div>
	<!-- 		<%= link_to "Edit", edit_topic_path(@topic), class: 'btn btn-success' %> |
			<%= link_to "Back", topics_path %>
		</div>
	</div>
</div> -->
